{
    "tasks": [
        {
            "type": "cppbuild",
            "label": "C/C++: gcc build active file",
            "command": "/usr/bin/gcc",
            "args": [
                "`pkg-config",
                "--cflags",
                "gtk+-3.0`",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}",
                "${file}",
                "`pkg-config",
                "--libs",
                "gtk+-3.0`"

            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Task generated by Debugger."
        }
    ],
    "version": "2.0.0"
}